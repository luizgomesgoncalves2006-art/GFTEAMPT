const form=document.querySelector('[data-contact-form]');if(form){form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const name=form.elements.nome.value.trim();const category=form.elements.categoria.value;const message=`Olá! Meu nome é ${name}. Gostaria de informações sobre aulas de jiu-jitsu da GFTEAM Portela${category?` para ${category}`:''}, horários, valores e pagamento.`;window.open(`https://wa.me/5521967455044?text=${encodeURIComponent(message)}`,'_blank','noopener');form.querySelector('.status').textContent='Abrindo o WhatsApp para continuar o atendimento.'})}

const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const lightbox=document.createElement('figure');lightbox.className='lightbox';lightbox.hidden=true;lightbox.innerHTML='<button class="lightbox-close" type="button" aria-label="Fechar imagem">×</button><img alt=""><figcaption></figcaption>';document.body.append(lightbox);let lastFocus=null;function closeLightbox(){lightbox.hidden=true;document.body.style.overflow='';lastFocus?.focus()}document.querySelectorAll('[data-lightbox]').forEach(btn=>btn.addEventListener('click',()=>{lastFocus=btn;lightbox.querySelector('img').src=btn.dataset.lightbox;lightbox.querySelector('img').alt=btn.querySelector('img')?.alt||'';lightbox.querySelector('figcaption').textContent=btn.dataset.caption||'';lightbox.hidden=false;document.body.style.overflow='hidden';lightbox.querySelector('button').focus()}));lightbox.querySelector('button').addEventListener('click',closeLightbox);lightbox.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!lightbox.hidden)closeLightbox()});
